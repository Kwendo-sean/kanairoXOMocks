import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF8B1A1A);
  static const primaryLight = Color(0xFFB22222);
  static const primaryGlass = Color(0x268B1A1A);  // 15% opacity

  // Surfaces
  static const background = Color(0xFFFAF7F4);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceGlass = Color(0xB3FAF7F4); // 70% opacity

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6B6B);
  static const textMuted = Color(0xFFA0A0A0);

  // Glass borders
  static const glassBorder = Color(0x4DFFFFFF);  // 30% white
  static const glassHighlight = Color(0x33FFFFFF); // 20% white
}
